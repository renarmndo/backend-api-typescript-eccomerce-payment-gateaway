export class CustomError extends Error {
    msg;
    status;
    constructor(msg, status) {
        super(msg);
        this.msg = msg;
        this.status = status;
        this.status = status;
    }
}
//# sourceMappingURL=CustomerError.utils.js.map